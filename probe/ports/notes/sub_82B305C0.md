# sub_82B305C0

**Never called in the traced boot session** -- one of four functions in the 216 audio-thread set
with a boot count of 0, so the boot session will not exercise this port. It has no direct caller
in the census (reached through a pointer, if at all). `docs/execution-trace.md` does list it among
the five a *played* session added, so a play session may reach it; until one does, nothing has
compared it. STATUS stays `pending`, not `uncalled`, for that reason.

Non-leaf, 269 lifted lines. r3 = an unnamed object, r4 = the descriptor pair (`+28` = the source
descriptor, as `sub_82B29BE0.inc` names it). Returns 1 on every path: `kReturnR3`.

Flow: `obj+66 == 0` returns at once. Otherwise increment the u32 at `*(obj+52) + u16(obj+64)`,
then (loaded after that store) read `obj+12`, `pair+28` and the mode byte `*(obj+12)+72`. For
`obj+102` channels k (count, `+100` and `+101` reloaded every iteration):
`src = desc.base + 4*(desc.stride*(obj[100]+k))`, `dst = *(obj+52) + 1024*(obj[101]+k)`; mode 1
calls `sub_82B44D18(dst, src)` with f1/f2 = singles at 0x8231A844/0x822F8A4C, mode 3 the same with
0x82165A10/0x822F87E0, any other mode `sub_82B44B20(dst, src, r6=256)` with f1 = 0x8231A844. Then
`obj + 4*(obj[101]+k+17)` = `src[255]` (stfsx). f1/f2 are loaded once and never reloaded: later
iterations pass whatever the kernel left, so the port reads them off the live context (neither
kernel's lifted form writes them).

Stores: counter (4); `obj+68+4*(obj[101]+k)` (4 each); through the kernels, per their verified
`Windows()` (mirrored from `sub_82B44D18.inc` and `sub_82B44B20.inc`): `[dst & ~0xF, +1024)` for
the ramp and `[dst, +1024)` for the scale, abutting across k, so one span.

Window: 3 spans, at most 4 + 32 + 8 KB. Returns false when `obj[101] + obj[102] > 8`: the
last-sample stores then reach `obj+100..+102`, the bytes the loop reloads. Also false when the
counter store would land on `obj+12`, `pair+28`, the mode byte, `obj+100..+102` or the
descriptor, when a run or last-sample store overlaps `obj+100..+102` or the descriptor's base or
stride, on a null object/pair/descriptor, or when a run wraps 2^32. `stwu r1,-128` reproduced
(both kernels spill below their r1); r27-r31 are never written by the body.

Gates: 1 pass (two verified leaf kernels, no lock, no pointer call), 2 pass under the guards, 3 pass,
4 pass. Unsure: what the mode byte and the counter mean. `*(obj+12)+72` is the offset where
`sub_82B335A8` stores its header's 4-bit second field, but nothing shows it is the same record.
