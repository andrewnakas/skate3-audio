# sub_82B1D118

Leaf, 74 lines, no callees, no stores, no timebase. Argument: r3 = object `{u8 count +0, u32
elements[] +4}`. Returns in r3 the 64-bit sum of `elements[0..count-1]`, each a zero-extended
u32 load. Element 0 is read and returned unconditionally, even when count is 0 or 1 (bug for
bug). For count >= 3 the original sums pairs (`((count-3)>>1)+1` iterations of `lwz`/`lwzu`)
into two accumulators r7/r6, then for even count adds the last element via `lwzx`, then folds
`r6+r7` into r3. All adds are u64 on wraparound, so the port keeps the same grouping and the
result is bit-identical over 64 bits regardless.
Structure unnamed: docs/rw_audio_structs.h has nothing at this shape; offsets kept as plain
constants. Guess only: a per-channel or per-segment sample count table being totalled.
Stores: none. Frame: none (no `stwu`, no red-zone spills).
Window: none. Read set `{r3, 4 + 4*max(count,1)}`. Result: `kReturnR3`.
Gates: 1 clean (leaf), 2 clean (empty write set), 3 clean (no `mftb`), 4 satisfied by r3 --
no static caller exists in the lifted tree (only `DECLARE_REX_FUNC` in skate3_init.h), so it is
reached through a function pointer; the consumer could not be read, but the body computes a
single value into r3 and uses r4-r11 only as scratch, the standard ABI result shape.
boot: 181,745 calls, RwAudioCore Dac.
