# sub_82B453D8

Leaf, 66 lines, float only, no callees, no imports, no timebase. ~77.7K calls per boot session on
`RwAudioCore Dac`. Ten direct callers (skate3_recomp.67.cpp:21517, 68.cpp:30561..30974). Every one
reloads what it needs from memory afterwards (`lfs f2,0(r31) ; lfs f1,4(r31)`, or a fresh `lfs f0`),
so none reads f0/f11/f12/f13. Void: mask `kReturnNone`. f1/f2 are read, never written.

Arguments: r3 = 12-byte output record, f1 = x, f2 = y.
Constants (image dump): 1.0f at 0x8231A844, 0.999f (0x3F7FBE77) at 0x820ED800.

Behaviour: store x, y, then lsq = fmadds(x,x,fmuls(y,y)) at +8. If lsq < 1: when lsq > 0.999 overwrite
+8 with 1.0 (x/y NOT rescaled), else leave it. If lsq >= 1 or NaN: when lsq > 1, len = fsqrts(lsq),
+8 = 1.0, s = fdivs(1,len), +0 = fmuls(s,x), +4 = fmuls(s,y). NaN and lsq == 1 return after the
first three stores.

Stores, in order: `r3+0`, `r3+4`, `r3+8` (all paths); then `r3+8` (snap), or `r3+8`, `r3+0`, `r3+4`
(normalise). All singles. Both `disableFlushModeUnconditional()` points kept (entry, loc_82B45414).
The `fmadds` stays `std::fma`; every other op is a separate double op rounded to single.

Window: `{r3,12}`, unconditional. Reads: the two rodata singles only (nothing is loaded from r3).
Gates: 1 pass (leaf), 2 pass (12 bytes at r3), 3 pass, 4 pass (stores; callers read memory).
Unsure: the unit-disc reading assumes the two constants stay 1.0/0.999 (both are in rodata).
