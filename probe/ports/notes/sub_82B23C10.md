# sub_82B23C10

- Role: leaf `u64 f(obj*, u8 index)`: how much of stream `index` is left, i.e. element limit minus
  cursor, or 0 when the element's limit is 0. 47 lifted lines, no callees, imports, indirect
  calls, timebase or locks. 136,892 boot calls on `RwAudioCore Dac`.
- Arguments: r3 = object, r4 = index (only the low byte is used: `clrlwi r10,r4,24`). Verified
  in caller sub_82B29288 (lifted 67.cpp:28042): r3 = `lwz r30,336(r31)`, i.e. Player + 0x150
  (`rw_player.decoder`), r4 = `lbz r4,92(Player + 12*u8[Player+372])`, i.e. entry +8 of the
  Player's +0x54 table. The object itself is not named in `docs/rw_audio_structs.h`.
- Layout read off this function: `+28` u32 live cursor of the active stream, `+36` u32 byte offset
  FROM THE OBJECT of a 24-byte-stride element array (same idiom as sub_82B3CA60's
  `lwz r11,40(r3); add r29,r11,r3`), `+49` u8 active index; per element `+8` saved cursor,
  `+12` limit. Names are a reading.
- Address math: the two `rlwinm` give exactly `24*index` (index <= 255, nothing wraps or masks);
  the 64-bit adds feed only the u32 effective address, so u32 arithmetic is equivalent.
- Stores: none.
- Result: r3 = `limit - cursor` as a 64-bit subtract of two zero-extended u32 loads, so a cursor
  past the limit leaves `0xFFFFFFFF_xxxxxxxx`; the rewrite reproduces that. Cursor is `+28` when
  `index == u8[+49]` (unsigned `cmplw`), else element `+8`. Mask `kReturnR3`: all five direct
  callers (67.cpp:19754, 28042, 28181, 54754, 55252) consume r3 only and redefine r8-r11 before
  reading them.
- Windows: no writes; reads `+28`, `+36`, `+49` and the element's `+8..+15`. Always true.
- Gates: 1 pass (leaf), 2 pass (no writes), 3 pass (no mftb), 4 pass (r3).
- STATUS: pending. The `.inc` predates this note (written by an earlier run); it was re-read
  against the lifted body and its caller claims checked, and left unchanged.
