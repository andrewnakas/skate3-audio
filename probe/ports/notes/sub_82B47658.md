# sub_82B47658

Non-leaf, 60 lines. r3 = out (4 words), r4 = in (four 16-byte sub-objects at +4/+20/+36/+52).
For each sub-object it calls `sub_82B47010(obj)` and stores r3 at `out+4i`.

`sub_82B47010(obj)`: if `s32[obj+8] <= 0`: `sub_82B46EA0(*(*obj), &scratch)` decodes a
variable-length code (1-5 bytes, prefix thresholds 192/240/252/255) and returns its length;
then `*(*obj) += len`, `[obj+8] = scratch+1`, `[obj+12] = 1`, with a sign fix-up when the
decoded count is negative; if the flag is set, `sub_82B46FB0(obj)` decodes another code into
`[obj+4]` and advances the cursor. Then `sub_82B46FB0` again if the flag is clear. Finally
`[obj+8] -= 1` and returns `[obj+4]`.

Writes from entry state: `{out,16}`, per sub-object `{obj+4, 9}` (accumulator, remaining,
flag) and `{*obj, 4}` (the cursor's byte pointer). `sub_82B46EA0` writes only its caller's
stack scratch. Gate 1: all three callees are leaves or pure memory; no indirect calls.
Gate 2: cursor pointer read before the call, never rewritten during it.
Pilot 3: a non-leaf calling a still-lifted callee through GuestCall. The callee's census
count doubles under shadow (lifted run plus native replay) until it has its own port hook.
