# sub_82B21D70

Twenty-five lifted lines, of which twenty-one are the stack frame. The whole function is:
build a frame, `bl 0x82b21ac0` with r3 untouched, `li r3,0`, unwind. It overrides the callee's
result with a constant zero, so it reads as the "run it and report success" wrapper around
`sub_82B21AC0` rather than a forwarder -- a tail branch would have been enough otherwise.

Arguments: r3 = the object `sub_82B21AC0` takes (it reads `+8`, `+88`, `+92`, `+96` off it);
r4 and up are not touched here.

Stores: none outside its own frame (`stw r12,-8(r1)` and the `stwu`, both excluded by the
brief). Everything written on this path is `sub_82B21AC0`'s.

Gate verdict: **gate 1**, at depth 1, `sub_82B21AC0`. Two independent reasons, either fatal:
it calls `__imp__RtlEnterCriticalSection` / `__imp__RtlLeaveCriticalSection` around its body,
and it has two `bctrl` indirect calls. Replaying the native body on rewound memory would take
the same critical section a second time and re-issue both indirect calls, whose targets are
whatever the loaded vtable slots point at -- outside the rewind window by construction. The
call is unconditional, so no path avoids it.

Write set that would have been declared if it were comparable: `sub_82B21AC0`'s, and that is
not enumerable either -- its provenance table shows two `stwx` stores inside loops with
loop-derived bases (lines 9529, 9571) and a global at `0x82044588` (`lis -31992` -> 0x82040000
+ 17864), so it would be gate 2 one level down as well. Result mask: r3, and the value is the
literal 0 regardless of what the callee returns.

Called once per boot session, matching `sub_82B21AC0`'s own single call -- so this wrapper is
that function's only caller, and it is reached indirectly (no `bl` to this address exists in
the generated corpus). A one-call-per-boot function is also the worst possible shadow subject
even without the gate: one comparison is not evidence.
