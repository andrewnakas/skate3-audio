# sub_82B1D098

Leaf, 72 lines, no callees, no stores outside its own red zone. Argument: r3 = a 12-byte object
`{f32 +0, s32 +4, s32 +8}`. Computes `float(float(s32[+8] -> f32) * (s32[+4] -> f32)) * f32[+0]`
in single precision, compares against the f32 at `0x82165A10` (0.0f per docs/command-queue.md),
subtracts the f32 at `0x8209975C` when below it, adds it otherwise (presumably 0.5f: round half
away from zero; the port loads it at run time and assumes nothing), then `fctiwz`. Returns the
LOW word of the spilled fctiwz result in r3, zero-extended (`stfd f13,-16(r1)` / `lwz r3,-12(r1)`).
Stores: `std -16(r1)` x2 (int->double bit copies for `fcfid`), `stfd -16(r1)` x2 (one per branch,
the fctiwz spill). All are below r1 with no `stwu`; treated as the function's own frame, not
windowed, not reproduced — the native body reinterprets in registers. Assumes no caller reads
the red zone afterwards.
Flush mode: `disableFlushModeUnconditional` kept at the `lfs f0` point and at `fsubs` (blt path
only), as lifted. fctiwz edge cases reproduced with the lifted expression (NaN -> 0x80000000,
> INT_MAX -> INT_MAX, else CVTTSD2SI). NaN compare: `cr6.lt` false -> fadds path, as original.
Window: none. Read set `{r3, 12}`, `{0x82165A10, 4}`, `{0x8209975C, 4}`. Result: `kReturnR3`.
Gates 1-4 clean (no callees, no timebase, result in r3). boot: 825,088 calls, RwAudioCore Dac.
Unsure: what the three fields mean (a seconds x rate x channels -> sample count is a guess only).
