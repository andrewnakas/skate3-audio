# sub_82B33870

Lifted: `skate3_recomp.67.cpp:52848`, 147 lines. Callees `sub_82B32550`, `sub_82B332D0`,
`sub_82B33418`. No imports, no `bctrl` of its own, no `mftb`.

## Arguments

- `r3` owner object. `+96` u32 = base of an array of 80-byte slots; `+464` u16 = the byte bias,
  measured from the object itself, of a parallel array of 48-byte records.
- `r4` slot index. Addresses `slot = *(owner+96) + index*80` and
  `record = owner + *(owner+464) + index*48`.
- `r5` passed straight through to `sub_82B332D0` on both deferral paths. It is never written
  before those calls, so it is an argument of this function even though nothing here reads it.

## Control flow

`state = u8 slot+73`

- `state == 0` -> advance.
- `state == 1` -> defer.
- otherwise -> advance iff `(s32)(u32 record+24) < (s32)(u32 slot+16)`. Signed compare
  (`cmpw` + `bge`), strictly-below only.

Deferral is `sub_82B332D0(owner, index, r5, 1, 0)`; the result's low byte is tested and this
function returns 1 when it is nonzero, 0 otherwise. The two deferral sites (the `state == 1`
fallthrough and `loc_82B33944`) set up an identical call; `loc_82B33944`'s `mr r4,r29` is a no-op
because nothing has clobbered r4 yet, so the port merges them.

The advance path returns 1 unconditionally.

## Stores

| address | size | mnemonic |
|---|---|---|
| `r1 + 80` (frame + 80) | 4 | `stw r10,80(r1)` -- zeroed, then passed by address |
| `slot + 52` | 4 | `stw r11,52(r31)` -- the handle read from `slot+48` before the call |
| `slot + 74` | 1 | `stb r9,74(r31)` -- low byte of the word `sub_82B32550` filled in |
| `slot + 48` | 4 | `stw r3,48(r31)` -- `sub_82B33418`'s return value |
| `r1 - 128` | 4 | `stwu r1,-128(r1)` -- own frame |

The frame is reproduced (unlike most ports) because the local at `r1+80` is passed by address to
`sub_82B32550`; without the `stwu` that store would land in the caller's frame.

`slot+48` is **reloaded** after the `stb`, not carried from the `lwz r11,48(r31)` eight
instructions earlier. Reproduced as a reload.

## Gate verdict

**gate-1.** `sub_82B33418` -> `sub_82B3C870` contains a `bctrl` (census: `gate1 fail,
reason indirect, at sub_82B3C870, depth 3`). The closure is 182 functions and leaves the audio
address range entirely (`82EE*`, `82F5*`), so rewinding it is not possible. `Windows()` returns
false unconditionally and the macro never enters the shadow branch.

Had it been comparable the write set would have been enumerable from entry state: the two array
bases and the state byte are all loaded before the first store, and no store this function makes
can change them. It would have declared `slot+48` (8 bytes, covering `+48` and `+52`),
`slot+74` (1) and the reads of `owner+96`, `owner+464`, `slot+16`, `slot+73` and `record+24` --
five spans, well inside budget. Plus every byte the three callees write, which is the part that
is not knowable.

## Uncertainties

- **Nothing exercises this function.** `calls/session: boot 0`. It is one of only four in the
  216-function audio-thread set that the traced boot session never called, so the body below has
  never run in any form and the thread attribution in the header is static, not observed.
- `sub_82B32550` leaves a second result in **r5**, which `mr r6,r5` reads and forwards as
  `sub_82B33418`'s fourth argument. That is an out-register, not a return value in the usual
  place; it is reproduced as a read of `ctx.r5` after the `GuestCall`, but the callee was not
  read to confirm what r5 means.
- `+464` is read as a **u16 byte bias added to the object pointer**, not as an element count or
  an index. That is what `lhz r11,464(r3)` / `add r9,r11,r3` does, but only this one site was
  read, so the field's meaning is a reading.
- The 80 and 48 strides come from `index*5*16` and `index*3*16`; neither record is named
  anywhere, and the `+16` limit versus `+24` counter pairing is inferred from the compare alone.
