# sub_82B29288

- Role: PacketPlayer output step, `u32 f(Player*, descriptor*)`. 626 lifted lines (67.cpp:27834),
  callees sub_82B29118 (leaf, the ramp), sub_82B23C10 (leaf), sub_82B3CA60 (decode). No direct `bl`
  anywhere reaches it; it is dispatched through a table. 1,683 boot calls on `RwAudioCore Dac`.
- Paths, in order: (a) `u8[+370]` and `u8[+369]` both nonzero -> tail into sub_82B29118(r3, r4),
  r3 is its result (1 on the path read). (b) playing (`+0x15E` neither 0 nor 4) and rate `+0x154`
  (f32, NaN-unequal) or channels `+0x15F` differ from `+360`/`+42` -> report and remember, r3 = 1.
  (c) otherwise walk the `+0x54` table from cursor `u8[+372]` (wrap at 20, stop at `u8[+371]`,
  unbounded as in the original) to a nonempty entry; if its state byte (+9) is 1: reserve
  `(u16[+348]+127)&~127` from `u32[u32[Player+8]+0]+32`, set `+324` = decoder `+0x150`, call
  sub_82B23C10(decoder, entry+8), then sub_82B3CA60(decoder, desc+32, signed-min(u16[+364],
  that)), swap desc+28/+32, add the result to `+356` and entry+4, and while the remainder's low
  word is 0 mark entries 2 and advance. Then undo the reservation if `+324` was set and the new
  budget is nonzero, report `+42`/`+360`, and either return 0/1 or copy each channel's last
  sample `samples[stride*c + consumed - 1]` into the float array at `Player + u16[+366]` and set
  `+369` = 1, r3 = 1.
- Stores: Player `+324` (4, up to 3x), `+372` (1, per step), `+356` (4), `+369` (1), `+42` (1),
  `+360` (4), table entry `+4` (4) and `+9` (1, per retired entry), `Player+u16[+366] + 4c`
  (4 each, c < min(desc+60, u8[+368])); pool `+32` (4, twice); descriptor `+28`, `+32`, `+48`
  (4 each), `+52` (4), `+60` (1). Own frame `stwu -160` plus savegprlr_24 spills: not windowed.
- Frame reproduced (the callees run on it); r24-r31 are never touched, so the epilogue only
  rebuilds r1, r12 and lr. XER.CA from subfic/subfe is not tracked (volatile, never read after).
- Gate 1 fails on ONE path: sub_82B3CA60's own two `bctrl` (0x82B3CB80, 0x82B3CCA0, depth 1),
  taken only when the entry the walk lands on is in state 1. Paths (a), (b) and (c)-without-
  state-1 call only leaves or nothing, and are predictable from entry state by replaying the
  walk (capped at 20 steps). Labelled gate-1 as instructed; a split Windows() would make those
  paths comparable -- worth doing if this function ever needs verifying.
- Mask `kReturnR3`: every path sets r3 (0/1, or sub_82B29118's 1). The table caller was not read.
- Unsure: all undocumented names (+42, +324, +348..+372, descriptor fields, the pool at
  `system->+0`) are readings from this one function; that +370 is a ramp length comes only from
  sub_82B29118 dividing by it.
