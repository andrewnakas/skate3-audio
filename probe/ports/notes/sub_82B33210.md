# sub_82B33210

Twenty-seven lifted lines, twenty-three of them frame. The body is three instructions:
`lwz r3,4(r3)`, `bl 0x82b32328`, `li r3,8`.

Arguments: r3 = a record whose second word (`+4`) is the object `sub_82B32328` operates on.
That callee writes a block of flags and counters on it -- `+432`, `+436` (words) and
`+467`..`+477` (bytes), plus `+468` inside a loop -- which reads as a stop/reset of a running
pattern. The constant 8 returned here appears verbatim in `sub_82B49210` and `sub_82B49238`
too, which share this exact three-instruction shape over different callees; that argues it is
a shared return convention (a status or record size) for one dispatch table rather than
anything specific to this function. I did not pin down which, and the address is called only
through a function pointer -- no `bl 0x82b33210` exists anywhere in the generated corpus -- so
the table itself was not available to read.

Stores: none outside its own frame.

Gate verdict: **gate 1**, at depth 2, `sub_82B33F98`. `sub_82B32328` is itself clean of
imports and indirect calls, but it calls `sub_82B33F98` from inside its loop, and that ends in
a `bctrl`. This is exactly the transitive case `docs/PLAN.md` warns about: a one-level screen
passes both this function and `sub_82B32328`. The closure is 183 functions wide and reaches
the allocator and the file system, so the indirect target is unbounded in practice as well as
in principle.

Write set that would have been declared if it were comparable: from `sub_82B32328`, with
`object = REX_LOAD_U32(r3 + 4)` --
`{object + 432, 8}` (the two words at +432 and +436) and `{object + 467, 11}` (covering
+467..+477, the union of the byte flags including the loop's +468). Reads: `{object + 464, 14}`
for the counters the callee keys off, plus the per-entry byte its loop test reads, at
`object + 46 + ((3 * b) << 4) + h` where `b` is the byte at `object + 468` and `h` the
halfword at `object + 464` -- that index advances with the loop, and it is the part that is not
statically bounded. Result mask: r3, the literal 8.
