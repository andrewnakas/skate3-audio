# sub_82B225A0

- Role: `void f(obj*)` — maintain this object's share of a running float total held on its owner.
  Mode 1 (`int32[+64] == 1`) computes the value from `int32[+140]` converted to float, corrected
  through `sub_82F55068` when the coefficient at `+92` is non-zero; any other mode settles at zero
  and just withdraws the last contribution. Both arms publish at `+28`, add `new - old` into
  `float[u32[+12]+40]`, and record the new value at `+32`. 135 lifted lines, one direct callee,
  closure of 2, no imports, no indirect call, no lock, no timebase. 111,801 boot calls,
  `RwAudioCore Dac`. One call site, `sub_82B222D8`, which ignores r3 and f1 (`li r3,1` follows).
- Arguments: r3 = the object. `u32[+12]` the owner, `float[+28]` the uncorrected value,
  `float[+32]` the last published value, `int32[+64]` the mode, `float[+92]` the coefficient,
  `int32[+140]` the length; `float[owner+40]` the running total.
- Stores, both arms: `self+28` (4 B), `u32[self+12]+40` (4 B), `self+32` (4 B). Nothing else above
  r1. The `std r9,80(r1)` / `lfd f0,80(r1)` pair is only the GPR-to-FPR round trip for `fcfid`, in
  this function's own frame; it is done natively as `double(int64_t(int32_t(length)))` and that
  frame word is never read again.
- The `stwu r1,-112(r1)` **is** reproduced: `sub_82F55068` allocates 96 bytes of its own and its
  callee `sub_82F54ED8` scratches at `r1+16`, i.e. *inside* 82F55068's frame, so the whole chain
  must sit below this function's frame rather than on top of it. The r12/r31/f31 spills are not
  reproduced — nothing here writes r31 or f31, and the callee chain touches only f0-f13 and r6-r11
  (checked), so the restores at exit are no-ops.
- `sub_82F55068` through `GuestCall`: it and `sub_82F54ED8` write only their own frames, so they
  need no windows. `sub_82F54ED8` is a table-driven transform over doubles at
  `0x82050000`-ish; `sub_82F55068` scales its result by a constant double. Which transform it is
  does not matter to the port — the value is not reimplemented.
- Float detail that must not drift: `fcfid` then `frsp`, so the value is
  `(float)(double)(int32)`; `fmuls` then `fdivs` are two single-rounded steps, not one; `fabs` and
  `fneg` are bit operations on the double, so `-0` and NaN payloads survive; and the `fcmpu` +
  `beq` against zero is an *ordered* equality, so a NaN coefficient takes the correction path.
  Constants, both computed rather than read by eye: `0x82165A10` (`lis -32234` + 23056 — the same
  word `port_82B269C0` calls kZeroFloat) and `0x821F1790` (`lis -32225` + 6032).
- Window rationale: three spans, 12 bytes — `self+28 .. +35` as one span plus `owner+40`. Every
  address is entry state: the owner pointer comes from `+12`, which no store in this function can
  alias (the +28 store precedes that load on the mode-1 arm). Returns false only when r3 is null.
  A null owner is reproduced, not guarded: the original would touch `float[0x28]`, and that word is
  declared, so the harness rewinds it.
- Result mask `kReturnNone`: r3 is never written, and the single caller overwrites r3 immediately
  after the call. f1 is left holding the transform result on one path, but it is volatile scratch
  the caller does not read.
- Gate verdict: **no gate.** Gate 1 passes (the census agrees: no allocation, lock, indirect call
  or timebase in the closure), the write set is three fixed words, and the result is observable.
  STATUS pending: written, never compared.
