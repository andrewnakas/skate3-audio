# sub_82F55068

Non-leaf, 30 lines, 61,483 calls in the verifying boot session on `RwAudioCore Dac` (75,560 in the
screening census). Compared clean, promoted to `kPortVerified`. It is the CRT's **log10**: call the
natural log, multiply by log10(e).

Argument and result are both f1; no GPR arguments. Six call sites in the lifted tree
(`skate3_recomp.67.cpp:11049,43322,43413`, `99.cpp:54594`, `106.cpp:42311,44493`); the one at
`67.cpp:43322` reads the result straight back through `frsp f12,f1`, so the mask is `kReturnF1`.

One callee, `sub_82F54ED8`, reached through `GuestCall(ctx, base, sub_82F54ED8)` with no arguments,
never `__imp__`. It is **outside the audio corpus** (`closure_outside: ["82F54ED8"]`) and had no
package, so it was extracted with `tools/extract_lifted.py --one 82F54ED8`: a leaf, no callees, no
imports, no indirect call, no lock, no `mftb`. It is the classic Cody-Waite natural log, and the
constants confirm it rather than the shape alone -- read out of `probe/harness/out/image/g_8200.bin`
at `0x82050000 + 8344` onwards they are sqrt(2)/2 = 0.7071067811865476, ln2 split as
0.693359375 - 0.00021219444005469057 (which sums to 0.6931471805599453 exactly), the rational
coefficients 769.4993210849487 / 312.03222091924533 / 64.12494342374558 / 35.66797773903465 /
16.383943563021536 / 0.7895611288749126, then 2^53, DBL_MIN and DBL_MAX. `0x82FB0000 + 22720` is
+inf and `+22728` is NaN, both negated on the way out for log(+0) and log(x < 0).

The multiplier this function applies, `lis r11,-32251 ; lfd f0,8368(r11)`, is
`0x82050000 + 8368 = 0x820520B0` -- computed as `((imm & 0xFFFF) << 16) + offset`, not read off --
and holds 0.4342944819032518, the correctly rounded log10(e). (Note it is one ULP away from what
`1/log(10)` produces in double, so it is a tabulated constant, not a computed one. The port loads
the word rather than writing the literal: a wrong guess would not be caught by a harness that read
the same word.) `fmul f1,f1,f0` stays a plain double multiply -- the lifted line is `fmul`, and
there is no adjacent add for a compiler to contract into an FMA.

Own frame: `stwu r1,-96(r1)` **is** reproduced, unlike most ports. `sub_82F54ED8` spills the
argument at `16(r1)` and its reduced value at `-16(r1)` and reads both back through integer loads
(`lhz`, `lfd` after `sth`/`std`). Without the frame, `16(r1)` would land in the caller's parameter
save area 96 bytes higher instead of inside this function's frame where the original put it. It is
still not windowed: every access is write-before-read, and the back-chain word the port stores is
the same value the lifted `stwu` stored. r1 is restored exactly, and the port touches no
callee-saved register (the original's only spill is its own LR slot).

Stores: none outside that frame, in the whole closure. `stores_present: false`,
`gate2_suspect: 0`.

Window: empty -- `Windows()` returns true with no write spans. Reads are recorded for replay only:
the natural log's pool `{0x82050000 + 8344, 144}` (which contains the log10(e) multiplier at
+8368), `{0x82010000 + 248, 24}` (f64 0.0 and 1.0), `{0x82010000 + 648, 8}` (f64 0.5) and
`{0x82FB0000 + 22720, 16}` (+inf and NaN).

Gates: 1 pass. 2 pass -- the write set is empty, which is enumerable. 3 pass. 4 pass, via
`kReturnF1`; a port with no writes must name a result register or compare nothing, and f1 is what
every call site reads.

Unsure: (a) `sub_82F54ED8` is not itself ported, so this port's verification covers the wrapper's
multiply and the frame, with the log body running lifted inside both runs. If the log is ported
later, the two will interact through `ShadowNestedReplay` the usual way. (b) The log body uses
`std::fma` in five places in its lifted form (`fnmsub`/`fmsub`/`fmadd`); that is the callee's
concern, not this port's, but it is the kind of thing `docs/vmx128-exactness.md` rule 4 says to keep
bit-checked rather than reasoned about.
