/**
 * @file        skate3_audio_ports_misc.cpp
 * @brief       Aggregator for the native audio ports in recomp/src/audio_ports/ (group misc)
 *
 * Edit the per-function .inc files, not this. The manifest is written by
 * probe/ports/queue.py manifests; ninja tracks the includes, so a change to one .inc
 * recompiles this one object and relinks.
 */
#include "skate3_audio_port.h"

#include "audio_ports/ports_misc.manifest.inc"
